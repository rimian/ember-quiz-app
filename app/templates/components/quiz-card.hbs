
<ol>
  {{#each @questions as |question|}}
    <li data-test-question={{question.id}}>
      <p data-test-question-question>{{question.question}}</p>
    </li>
  {{/each}}
</ol>

<button onclick={{action "buttonClick"}}>Complete the Quiz</button>
